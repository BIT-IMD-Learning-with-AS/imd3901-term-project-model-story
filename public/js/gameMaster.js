'use strict'

//Created from a Tutorial by Don McCurdy
'use strict'

AFRAME.registerComponent('game-master', {
    init: function () {
      const el = this.el;

      var bugPuzzle = false;
      var lightPuzzle = false;
      var picPuzzle = false;
      var plantPuzzle = false;
      var cleanPuzzle = false;

  
      // On click, send the NPC to the target location.
      el.addEventListener('click', (e) => {

      });
    }
  });